<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ABComm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ABComm" parent="Base.Theme.ABComm" />

    <!-- Futuristic Dialog Theme -->
    <style name="CyberDialogTheme" parent="Theme.Material3.Dark.Dialog">
        <item name="android:windowBackground">@color/cyber_gray</item>
        <item name="android:textColor">@color/cyber_cyan</item>
        <item name="android:textColorPrimary">@color/cyber_cyan</item>
        <item name="android:colorAccent">@color/cyber_cyan</item>
        <item name="colorPrimary">@color/cyber_cyan</item>
        <item name="android:fontFamily">monospace</item>
    </style>
</resources>